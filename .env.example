# ─── Backend ───────────────────────────────────────────────────────────────────
# Postgres connection string
# Format: postgresql://<user>:<password>@<host>:<port>/<database>
DATABASE_URL=postgresql://postgres@localhost:5432/swissdevmap

# Node environment
NODE_ENV=development

# Backend port (default: 3001)
PORT=3001

# ─── Frontend ──────────────────────────────────────────────────────────────────
# Mapbox public token — get yours free at https://account.mapbox.com
VITE_MAPBOX_TOKEN=pk.your_mapbox_token_here

# Backend API base URL consumed by the Vite frontend
VITE_API_URL=http://localhost:3001
VITE_API_KEY=sdm_your_api_key_here

# ─── API Keys ──────────────────────────────────────────────────────────────────
# API keys are stored (hashed) in the database — no env variable needed.
# To issue a new key, run:
#   npx ts-node backend/src/db/migrate-api-keys.ts       (first time only)
#   npx ts-node backend/src/scripts/generate-api-key.ts --label "Recipient Name"
#
# Clients must send the key in every request:
#   Authorization: Bearer <key>
